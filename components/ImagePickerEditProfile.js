import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const ImagePickerEditProfile = () => {
  return (
    <View>
      <Text>ImagePickerEditProfile</Text>
    </View>
  )
}

export default ImagePickerEditProfile

const styles = StyleSheet.create({})