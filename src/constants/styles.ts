import { COLORS, CUSTOMFONT_REGULAR } from "./theme";

export const STYLES={
    textInputContainer: {
        borderColor: COLORS.gray,
        borderWidth: 1,
        borderRadius: 5,
        paddingHorizontal: 10,
        fontFamily: CUSTOMFONT_REGULAR,
        fontSize: 16,
        color: COLORS.white,
        paddingBottom: 10,
        paddingTop: 10,
    }
}