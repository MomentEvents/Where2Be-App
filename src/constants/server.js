const productionserver = "https://api.momentevents.app";
const version = "/api_ver_1.0.0";

export const momentAPIVersionless = productionserver;
export const momentAPI = momentAPIVersionless + version;
export const QUERY_PER_BATCH = 20;
